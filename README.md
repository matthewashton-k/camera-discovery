# Features
Discovers cameras as they are plugged in and prints out the port they are on.

# Installation 

```cargo install camera-discovery``` or download the release.

# Usage

run ```camera-discovery``` and unplug and replug devices to see which ones are on which ports.
